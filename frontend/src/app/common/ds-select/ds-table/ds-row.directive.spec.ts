import { DsRowDirective } from './ds-row.directive';

describe('DsRowDirective', () => {
  it('should create an instance', () => {
    const directive = new DsRowDirective();
    expect(directive).toBeTruthy();
  });
});
